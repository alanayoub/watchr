<div class="W-task-list-container W-task-list">
    {{#each tasks}}
    <div class="w-listitem">
        <div class="w-title">title</div>
        <div class="w-latest">{{value}}</div>
        <div class="w-url"><a href="{{url}}">{{url}}</a></div>
    </div>
    {{/each}}
</div>